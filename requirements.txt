# Trust-Calibrated NDT case study — pinned for reproducibility.
# These are the exact versions used to produce every number and figure in Section VIII
# and Appendix A of the paper (Python 3.12.10).
numpy==2.5.1
pandas==3.0.3
scikit-learn==1.9.0
matplotlib==3.11.0

# Optional: only needed to run the open-weight LLM controller instead of the
# rule-based fallback. The reported results use the fallback and need none of these.
# torch
# transformers
# accelerate
